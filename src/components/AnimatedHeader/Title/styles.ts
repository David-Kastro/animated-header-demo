import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  title: {
    marginTop: 10,
    fontSize: 28,
    fontWeight: '700',
    color: '#000',
  },
});

export default styles;
